# 前置总览图：中文重绘说明

两图分别对应原稿 `chapters/assets/media/image1.png` 与 `image2.png`，保留原有“总体构架／想法来龙去脉”的功能和原第 0—9 章的构建顺序。它们是概念图，不是新增数据图或新计算结果。

## 图 A：RSM 总体构架

![RSM总体构架](rsm_architecture_cn.png)

*图A：RSM 总体构架。算术及非自治动力学研究提供启发；逆对数平方响应作为输入假设进入内部时钟与离散格点的候选模型。状态关联的逻辑描述与局部传播的空间描述分列，二者和量子关联、真实时空的物理对应仍待建立。下方列出目前已完成的时钟—格点双向反馈、长波连续近似和算术残差对照；ECS 表示拟议的工程组织方案，当前基准程序尚不等于完整ECS引擎。章号指向沿用原文顺序的中文稿。*

## 图 B：RSM 想法的来源与检验路径

![RSM研究路径](rsm_reasoning_cn.png)

*图B：RSM 想法的来源与检验路径。黎曼零点谱统计背景和作者已发表的非自治映射数值研究，启发但不唯一推出 $1/\ln^2(R/R_*)$ 候选响应。实线仅连接给定模型内已定义、推导或计算的环节；虚线表示启发、额外假设及尚待建立的物理桥接。现有八个真实零点块的终点能量交换未相对于预定替代集合显示特殊响应；此结论受检验量、对照匹配和检出能力限制。真实物质耦合及内部坐标与宇宙时间的标定完成后，才能进一步以原子钟、光谱和宇宙膨胀史检验特定物理模型。*

## 与原图对应的修改

| 原图表达 | 新图处理 |
| --- | --- |
| 确定性全局普朗克时钟 | 区分内部演化、数值步长与待标定的物理时间。 |
| 逻辑层已经解释纠缠 | 保留状态关联的描述构想；明确物理机制待建立。 |
| 物理层已经给出相对论因果性 | 保留局部传播和长波近似；不把模型传播参数直接等同实测光速。 |
| “Tension Resolved” | 改成未来观测检验与模型筛选。 |
| “数学必然迫使物理常数漂移” | 改成可检验的候选假设与虚线物理桥接。 |
| $1/\ln t$ | 按当前研究主线统一为 $1/\ln^2(R/R_*)$；物理时间对应仍是额外任务。 |

## 复现

```bash
python manuscript_cn/figures/make_overview_figures.py
```

脚本可在任意工作目录直接运行；输出写入脚本所在目录。仅依赖 Python 与 Matplotlib，不联网、不读取观测数据。每图输出 PDF、SVG 和 PNG；尺寸为 9.0 × 6.2 英寸，PNG 为 2160 × 1488 像素。PDF 嵌入 TrueType 子集，SVG 将字形转成路径，便于跨设备显示中文。

默认字体为本机 `Droid Sans Fallback`（`/usr/share/fonts/truetype/droid/DroidSansFallbackFull.ttf`），可通过环境变量 `RSM_CN_FONT` 指定其他中文字库文件。数学符号使用 Matplotlib 的 STIX 数学字库。正文标签 10.1—11 pt；插入单栏稿时建议宽度 15.5—16.5 cm。

数值状态依据 `RESEARCH_DIRECTION.md` 与 `experiments/arithmetic_residual_v01/` 的已有报告；本次重绘不改变其结果。
