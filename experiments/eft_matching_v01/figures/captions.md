# 图注与生成记录

由 `code/plot_results.py` 读取 `results/` 中已归档 CSV/JSON，以 Matplotlib 生成 PNG 及矢量 PDF。图中无外部图片、生成式图像或新增观测；语言使用英文便于后续英文稿复用。

**图1 — 条件 Wilson 系数预算及选定一环 RG 闭合。** `wilson_budgets_and_rg`。左：实相位的单系数 10% 局部二维力预算；测试 c、β 时固定 ν0=0，测试 ν0 时另两者为零。右：mG=1 eV 的 c 与 β² 单系数边界，在四个计算尺度比较；虚线错误地冻结反项，实线对同一有限边界同步运行反项。横向连线仅供阅读，未计算更多观测或重新求背景。预算为指定部门的条件要求，不是观测上限。

**图2 — 响应形状及精确系数退化。** `kernel_shapes_and_degeneracy`。左：mG=1 eV、实相位的三个 χ 方向核，各自按相对2U响应的最大绝对值归一化；右：采用 χ、y 两方向、1025个坐标的三列单位范数响应矩阵之奇异值。Kc+2Kβ²−Kν=0，第三个奇异值是解析零关系的浮点残差。此退化只在保留sd及B²的选定势内成立，不代表整个作用量或粒子谱等价。
