# 执行记录与存储约定

2026-09-24。科学协议在首次背景、CAMB或PM计算前冻结，SHA256为 `74a2cab351d53f98169b885542d23d18b4d425c6fd6b75b91b49f5c53422e2e1`。以下记录实现细节，不改变模型、初态、运行矩阵、读数或预定阈值。

- CAMB 2.0.4安装到本次环境的隔离依赖目录；仓库脚本不依赖该绝对路径。生成的标准线性谱随仓库保存，复算背景和PM可直接使用保存输入。
- PM首次40项检查全部通过；作者侧独立检查建议拒绝非有限谱、非有限状态及非Hermitian位移，随后添加输入保护和3项拒绝检查。最终43项检查全部通过，两个attempt文件均保存。前一次哈希对应加保护前的代码，不能要求它与最终文件相同。数值演化、阈值和物理参数没有因此改变。
- 8个生产案例使用4个线程调度独立运行；每条轨迹内部FFT使用1个worker，OMP/BLAS设为1。案例共享只读初态、各自复制状态。记录的wall time是在并行负载下取得，不能用它作不同分辨率或其他引力代码的速度基准。
- 粒子演化、质量诊断及功率计算使用float64。存档的五时刻密度和二维投影使用float32压缩，以控制仓库大小；功率数组保留原float64读数。因此从存档密度重新计算功率会有小的存储量化差，独立核对须显式容许这项差别。图像读取存档场，不另插值构造演化。
- 初始模态保存为复数数组，32³和64³在各自胞中心取同一连续带限位移。没有为更明显的结构增加幅度，未修改外部CAMB谱或重新拟合SMICA。
- 生产结果的时间、空间差异分别对原始功率和配对响应计算。它们是指定两网格/有限步长的诊断；既不是严格连续极限误差界，也不是来自观测的置信水平。

- 低波数比较在首次生产前的代码中实现为按每个保存频箱的平均波数选择 `k <= 0.3`，不是对箱内所有Fourier模式重新截断。入选两个箱的平均波数为0.160365、0.280331 h/Mpc；第二箱的最高实际模式约0.3078 h/Mpc。结果、阈值和箱边界未根据结果改变，正文与图注按这一实际读数说明。
- 归档的背景CSV保留Python CSV写入器的CRLF记录结束符，CAMB参数文本保留上游表示的尾部空白；目录内`.gitattributes`明确这两种格式，并禁止相关换行转换。没有为通过代码空白检查而改写已经登记哈希的科学输入或结果。
